import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appweaver.template',
  appName: 'App Weaver Template',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
